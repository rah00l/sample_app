<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div> 

  <div class="field">
    <%= f.label :tag_name %>
    <%= f.text_field :tag_name %>
    <%= javascript_tag "observeAutocompleteField('movie_tag_name', '#{escape_javascript search_for_tag_tags_path()}')" %>
  </div> 

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
